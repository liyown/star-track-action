# Use with your local environment; never commit credentials.
GITHUB_TOKEN=
# npm run collect reads star-track.yml and only updates local files.
# npm run collect -- --gh uses your existing authenticated GitHub CLI instead.
